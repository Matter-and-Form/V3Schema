syntax = "proto3";

package MF.V3.Descriptors.Network;

/// Network interface struct.
message Interface
{
    /// The name of the interface.
    string name = 1;

    /// The address associated with the interface.
    string ip = 2;

    // The ssid or name of the network
    string ssid = 3;
}
