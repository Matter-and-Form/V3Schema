syntax = "proto3";

package MF.V3.Descriptors.Settings;

// Software settings descriptor.
message Software
{
    // Nightly releases included descriptor.
    message NightlyIncluded
    {
        bool value = 1;
        bool default = 2;
    }

    // Nightly releases included.
    NightlyIncluded nightlyIncluded = 1;
}
