syntax = "proto3";

package MF.V3.Settings;

// Software update settings.
message Update
{
    // Update major version which can have breaking API changes.
    optional bool majorVersion = 1;

    // Permit nightly release uppdates.
    optional bool nightly = 2;
}
