syntax = "proto3";

package MF.V3.Settings;

// Software settings.
message Software
{
    // Enable major version updates which can have breaking API changes.
    optional bool updateMajor = 1;
    
    // Enable nightly release candidate updates.
    optional bool updateNightly = 2;
}
